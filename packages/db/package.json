{
  "name": "@assistant/db",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "test": "node -e \"if (!process.env.DATABASE_URL) { console.error('DATABASE_URL is required for @assistant/db tests'); process.exit(1); }\" && vitest run",
    "test:capture:schema": "node -e \"if (!process.env.DATABASE_URL) { console.error('DATABASE_URL is required for @assistant/db tests'); process.exit(1); }\" && /home/harish/.local/node-v22.14.0-linux-x64/bin/node ./node_modules/vitest/vitest.mjs run src/schema/__tests__/capture-schema.test.ts"
  },
  "dependencies": {
    "@assistant/domain": "workspace:*",
    "drizzle-orm": "^0.44.7",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@types/pg": "^8.11.10",
    "vitest": "^3.2.0"
  }
}
